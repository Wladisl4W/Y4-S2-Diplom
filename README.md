# Тренировка интонации

Локальное десктоп приложение для начинающих певцов. Оно показывает текущую ноту, отклонение в центах и историю высоты голоса. Есть четыре паттерна распевок в трёх октавах, набор из них, обратная связь и локальная история занятий. Анализ MP3 пока исследуется и не выдаёт непроверенную нотную линию.

## Запуск

Нужен JDK 21. Для запуска из исходников выполните `./gradlew run` (Windows: `gradlew.bat run`). При первом запуске приложение предложит выбрать микрофон: выберите устройство, нажмите **«Проверить микрофон»**, спойте протяжную ноту и убедитесь, что появилась распознанная нота. Затем нажмите **«Сохранить и начать»**. Устройство запоминается на этом компьютере и при следующих запусках включается автоматически. Сменить его можно через **«Настройки»**. Если устройство отсутствует или недоступно, приложение покажет сообщение и предложит открыть настройки.

На главном полотне **«Живой голос»** вертикальный курсор закреплён по центру; сетка и линия голоса движутся относительно него. Кнопка **«Пауза / Продолжить»** останавливает и возобновляет полотно вместе с текущей распевкой. Каждая строка сетки охватывает диапазон одной ноты (±50 центов), а границы проходят между нотами. В каталоге **«Распевки»** выберите паттерн или набор, затем высоту C3, C4 или C5. Нажатие карточки сразу открывает главное полотно с целевыми нотами и двухсекундным отсчётом. Кнопка «Повторить на полотне» запускает выбранное упражнение ещё раз; образец можно прослушать отдельно. Набор состоит из четырёх паттернов подряд. Целевые полосы и линия голоса движутся по одной шкале времени. Зелёный участок линии означает попадание в диапазон ±50 центов, коралловый — промах. Кнопка «Прервать» отменяет попытку. Результат сохраняется локально в `~/.intonation-trainer/progress.csv`; последний результат показан под графиком, остальные доступны через «История».

Микрофон должен поддерживать моно PCM 44,1 кГц. Если приложение не видит устройство, проверьте разрешение на микрофон в настройках ОС и обновите список в настройках приложения.

## Разработка и проверка

`./gradlew test` выполняет тесты детектора, нотной шкалы, графика и упражнений. `./gradlew build` собирает приложение и запускает тесты. Пользовательские аудиофайлы и локальные результаты исключены из репозитория.

Документы: [архитектура](docs/architecture.md), [дизайн](docs/design.md), [методика качества](docs/quality.md), [этапы](docs/roadmap.md), [эксперимент MP3](docs/mp3-experiment.md), [решения](docs/decisions/0001-java-local-processing.md).

## Пакеты

Текущая версия отображается в названии окна и внутри приложения. Для локальной проверенной сборки выполните `./gradlew localRelease`; задача запускает тесты и создаёт установщик текущей ОС. Версию меняют в одном месте: свойство `version` в `build.gradle`.

На Linux автономное приложение запускается файлом `build/jpackage/Intonation Trainer/bin/Intonation Trainer` после `./gradlew jpackageImage`. Команда `./gradlew jpackage` создаёт установщик `.deb` в `build/jpackage/`. Оба варианта включают Java и не требуют установленного JDK. Для DEB на Linux требуется `fakeroot`. Ручной workflow в GitHub Actions собирает установщики трёх ОС как артефакты. Их необходимо проверить на реальном оборудовании перед публикацией релиза; порядок описан в [docs/releases.md](docs/releases.md).

## Ограничения

Первая версия рассчитана на один голос без аккомпанемента в диапазоне 80–1000 Гц. Микрофон обрабатывается локально, без записи и передачи звука в сеть. Анализ MP3 и пение по песне будут добавлены после проверки качества выделения вокала и извлечения нот на контрольных файлах.
